# Live Webcam Feed

A simple static page that uses the browser's `getUserMedia` API to show
your own webcam feed live in the page. Nothing is uploaded or recorded —
video stays entirely client-side in your browser.

Hosted via GitHub Pages.
